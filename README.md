# User Management with Mongo DB

Steps to start : 
* Start mongo db on port : `27018` and create database`users` 
* Install dependancies `npm install`
* Run `./start.sh` file

### API documentations are in  `postman_collection.json` file

